import { LIGHT_BACKGROUND_COLOR } from "@/colors";
import Phaser from "phaser";

export const REWORD_WIDTH_PX = 500;
export const REWORD_BOARDER_SIZE_PX = 20;

export const TITLE_FONT_SIZE_PX = 96;
export const DESC_FONT_SIZE_PX = 32;

export const REWORD_HEIGHT_PX = 1000;

export const REWORD_BACKGROUND_COLOR = Phaser.Display.Color.HexStringToColor(
  LIGHT_BACKGROUND_COLOR,
).color;

export const REWORDS_GAP_PX = 50;

export const REWORDS_BACKGROUND_COLOR = 0xffffff;

export const REWORDS_BACKGROUND_ALPTA = 0.5;